import React from 'react';
import { useQuery } from '@apollo/client';

export default function Ip(): React.ReactElement {

    return (
        <>
            <div className='glass-panel'>
                <div className='test'>IP ADDR</div>
            </div>
        </>
    );
}
